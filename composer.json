{
    "name": "yb/soap-server",
    "description": "Example SOAP server for testing purposes",
    "type": "project",
    "license": "MIT",
    "require": {
        "laminas/laminas-soap": "^2.11"
    },
    "require-dev": {
        "phpunit/phpunit": "^9"
    },
    "autoload": {
        "psr-4": {
            "SoapServer\\": "src/"
        }
    },
    "authors": [
        {
            "name": "Yvonne Breuer"
        }
    ],
    "scripts": {
        "serve": "php -S localhost:8000 -t public",
        "test": "vendor/bin/phpunit test"
    }
}
